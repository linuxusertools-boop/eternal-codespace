services:
  - type: web
    name: eternal-codespace-monitor
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node render-monitor.js"
    healthCheckPath: /ping
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
